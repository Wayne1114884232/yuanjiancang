# 元件仓 2.2.4

这版重点处理两件事：芯片标签识别不准时可以自己选取识别文字，入库记录可以按实际到货时间追溯。

## 主要变化

- 拍标签时增加“关键参数 / IC 型号·标签文字”模式。选择 IC 模式后，识别原文会拆成可点击的文字片段；点选型号、封装、数量、品牌或批次后可以人工改正，再填入对应字段。
- 关键字段显示“自动提取、选取文字、手工修改”等来源提示，仍保留人工确认步骤，不会把 OCR 结果直接记入库存。
- 标签入库支持“入库并拍下一袋”，自动沿用上一袋的地点、库位和供应商，适合连续整理一批淘宝或散料标签。
- 标签入库、手动入库、BOM 入库/出库、采购到货和新建元件初始库存都支持补录实际操作时间。系统另外保存不可修改的登记时间，方便区分“什么时候发生”和“什么时候上传”。
- 元件详情增加入库追溯，出入库记录增加操作时间、系统登记时间和日期筛选，流水导出也包含这些字段。
- 出库增加常用数量和“本库位全部用完”快捷操作，并支持保存后继续领下一种。
- 项目拣料单按地点/库位展开，支持勾选拿料状态和导出，项目领料仍由原有整单确认操作扣减库存。
- 统一出库分配顺序与拣料单顺序，减少“界面显示一个库位、实际领料扣另一个库位”的情况。

## 时间规则

时间输入留空时使用提交时间；补录时间只能早于当前时间，不能改变库存流水的提交顺序。旧数据没有系统登记时间时，界面会显示“旧版未记录操作人”或使用原流水时间兼容展示。

## 验证

- 全部自动测试和新增接收追溯测试通过。
- 新增覆盖：IC 文字选择、数量/精度校验、实际入库时间、系统登记时间、备份校验、项目拣料分配和预留库存保护。
- 尚未用用户真实 IC 料袋在 Android 真机上验收 OCR 准确率；识别结果仍需要人工确认。
