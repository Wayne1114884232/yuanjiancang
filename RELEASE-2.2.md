# 元件仓 2.2.0-beta.1

默认服务：https://yuanjiancang.onrender.com/ 。APK versionCode 12，应用名“元件仓”，沿用多人版签名及应用身份，可以覆盖更新。电脑与手机使用同一账号、同一元件库。

## 本次改进

- BOM 导入后可直接保存项目，缺失元件建立零库存档案。规格冲突阻止匹配，未知参数与差异显示在保存前核对表中；不产生入库流水，不增加数量。
- 元件库勾选 / 全选后有“全部用完”“填写数量领用”“批量损耗”三种操作。逐库位填数量，0 表示跳过；提交前显示当前与提交后数量。
- 离线草稿支持全选、顺序核对和整批提交，盘点检查记录时基准。整批异常不写入；损耗 / 盘点遵循敏感审批。返回结果丢失时保留原请求，先查询 / 重试原批次，避免再次扣账。
- 手机底部为首页、元件、记账、项目、我的；快捷记账可登记损耗、盘点、BOM；最近元件可以直接领用。
- 库存档案与手动关注的 IC 均可按立创编号刷新报价。国际 USD 与国内手动 CNY、报价与实际采购价分开；显示适用购买数量档、来源、时间、过期提示与失败。采购链接仍进入国内立创商城。
- 账号支持修改密码、退出其他设备、一次性恢复码找回；不需要邮件服务。恢复码只显示一次，只存摘要，用后失效。
- “我的”显示界面与 APK 版本、查询云服务与可下载 APK 版本、最近同步时间、独立备份提示（每7天），导出时间是本设备发起时间，需自行确认下载保存成功。

## 测试与边界

54项自动检查通过；包含真实 PostgreSQL 重启与并发、完整批次回滚、丢失响应后重试不重复扣账、恢复码跨重启单次使用、撤销会话、地点权限与管理员审批。真实 Edge 浏览器验证手机尺寸与电脑尺寸操作，并保存页面截图。Android APK 已构建和验签；尚未进行手机实机验收。

服务端改动须 GitHub 推送并由 Render 部署后才生效。免费服务可能休眠，在线更新受网络与唤醒时间影响。断网保留快照，新增记账为待同步草稿；登录后的库存并非永远在线。

价格为手动刷新及应用内提示；未实现休眠期间定时抓价或系统推送。国际接口返回的 USD 不会转换成或冒充国内 CNY 成交价。厂商停产信息需要有来源的通知记录，本版未配置自动厂商消息源。
