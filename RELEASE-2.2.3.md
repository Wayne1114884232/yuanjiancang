# 元件仓 2.2.3

本版针对拍照识别不准、慢的问题调整了识别流程。

- 拍照后先框选一个料袋或标签区域，去掉桌面、其他料袋和无关文字。支持拖动框、旋转90度。
- 电阻、电容、电感优先识别阻值/容量、精度、封装和数量；品牌、批次、供应商不再作为自动识别的可信字段，入库前可手工补充。
- 对 OCR 常见错误做纠正，例如全角小数点、全角百分号、Q TY、数字之间的空格、0402 被拆成 0 4 0 2。
- 手机 APK 使用内置中文文字识别模型，照片在手机本地处理，不上传标签照片；网页浏览器使用云端 OCR 备用。
- 关键字段为空时高亮提示，不会因为错误的品牌或批次自动建立错误档案。确认前仍可手工修改，确认后才入库。

建议：一次只拍一个料袋，标签铺平、正对镜头、避免反光；关键参数看不清时直接手工填入。

验证：64 项自动测试通过；真实 Edge 手机尺寸流程验证了框选、旋转、参数修改、快捷精度按钮、延迟结果丢弃和入库。合成标签已通过 Windows OCR 实际识别；手机本地接口使用模拟返回验证，无照片上传。ML Kit 的实际识别速度与真实料袋准确率尚未经过安卓真机验证。

安装包：2.2.3 / 内部版本号15，支持 Android 6.0 及以上的 ARM/ARM64 手机。内置识别模型使安装包增大，但控制在旧版升级器的32MiB限制内，不需要识别次数付费。

构建：本工作区可执行 android/build-cloud-apk.mjs 生成 Gradle 工程，再编译和使用原签名覆盖发布；原生识别依赖和架构配置见 android/ocr-build-config.json。
