# 元件仓 2.2.2

本版优化“扫一扫 / 拍标签入库”：支持淘宝、立创及其他供应商的包装标签，不要求立创采购编号。

- 识别并分栏填写阻值、容量、精度、封装、数量、品牌、批次，以及耐压、介质、功率、供应商等选填信息。
- 例如“阻值4.7K，精度1%，数量100，封装0402，品牌xxx，批次xxx”，可按参数创建电阻档案并入库100个。
- 没有型号的电阻、电容、电感可按参数生成内部编号；有型号时保留型号。批次不参与元件编号。
- 先对照照片修改字段，再核对入库清单，最后确认入库。未确认不改变库存。
- 可选择已有元件增加数量；明确型号或参数冲突时阻止合并。新档案编号重复时提示选择已有档案。
- 批次和供应商随每次入库流水保存、查询、导出和备份，同一元件后续入库不会覆盖旧批次。
- 无法确定的数量、封装等留空提示补充；不把包数、盘数、批号或价格猜成数量。

使用入口：首页 → 扫一扫 / 拍标签 → 拍标签识别入库 → 拍照或选图 → 核对字段和存放地点 → 核对入库清单 → 确认入库。也支持粘贴标签文字后提取参数。

照片送到元件仓服务进行 OCR，临时照片处理后删除；识别原文保存在本次入库记录中。确认前可以对照原图；本版不长期存储原照片，不支持按批次计算剩余库存，不自动解读电阻色环或无单位的三位电容编码。每次建议拍一个料袋。

验证：62 项自动测试通过；真实 Edge 浏览器手机尺寸流程验证了修改、返回核对、两次到货、参数冲突拦截，以及合成标签图片经 Windows OCR 的上传识别和 PostgreSQL 入库。尚未用实体安卓手机或真实供应商照片验收；Render 使用 Tesseract，与 Windows OCR 的识别效果可能不同。

APK：2.2.2，内部版本号14，包名 com.wayne.componenthub.team；名称仍为“元件仓”，默认服务 https://yuanjiancang.onrender.com/ 。2.2.1 用户可在设置中检查更新后覆盖安装；安卓安装确认需本人操作。
