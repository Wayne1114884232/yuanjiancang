# 元件仓 2.2.1

应用名称仍为“元件仓”，默认地址 https://yuanjiancang.onrender.com/ 。APK 内部版本号 13，沿用多人版应用身份和签名，可覆盖更新。

## APP 内升级

启动 APP 会在后台检查新版本；回到前台最多每6小时检查一次。发现更高的 APK 内部版本号时，弹出版本号和更新内容，可以“立即更新”或“稍后提醒”。稍后提醒对该版本延后24小时，手动检查不受影响。

立即更新会在 APP 内显示下载进度，下载完整后核对大小、SHA256、应用包名、版本号和签名，然后打开安卓安装确认。首次可能需要允许“元件仓”安装应用；不需要到浏览器查找下载地址，但仍需本人确认系统安装。失败时保留现有版本，可在“我的 → 检查更新”重试。

旧版 2.1.x 和 2.2.0-beta.1 的 APK 内没有自动升级程序，而且界面由安装包内文件提供，因此无法只靠服务端更新给这些旧 APK 补上弹窗。需要最后一次手动覆盖安装 2.2.1，此后发布更高版本即可使用 APP 内更新。

## APP 所有者协助找回账号

按用户指定的元件仓账号配置 APP 所有者。服务首次启用此功能时，将已存在账号的固定用户 ID 绑定到数据库；普通元件库的所有者、管理员不会获得平台账号找回权限。若配置账号不存在，代发功能保持禁用，之后新注册同名账号也不能自动获得此权限。

APP 所有者在“我的 → 为他人办理账号找回”填写目标账号、自己的当前密码和身份核实说明，确认已经核实是本人申请，生成绑定该账号的30分钟一次性恢复码。只显示一次，私下交给本人。重新签发会使上一枚代发码失效，不影响原来保存的个人恢复码。

申请人在登录页“登录遇到问题？→ 使用恢复码找回账号”填写账号、恢复码和新密码。成功后全部旧会话失效，个人与代发恢复码均作废。正常修改密码也会作废未使用的代发恢复码。签发与使用记录只有 APP 所有者可查看，不记录恢复码明文或密码。

## 验证范围

自动检查覆盖账号权限、过期和一次性恢复码、重启持久化、旧会话撤销、库存不变以及原有库存和协作流程。真实 Edge 浏览器验证签发、申请人重置、普通库所有者无代发入口与记录查看。APK 完成编译与签名验证。

安卓原生升级弹窗、安装来源授权和系统安装页面尚需在真实手机上验收；浏览器中的按钮测试只证明请求已转交原生更新入口。免费云服务唤醒或网络异常时，自动检查安静失败，手动检查会显示重试提示。
