# 元件仓 · 免费云端试用版 2.1.0-beta.2

手机 APK、电脑浏览器和其他成员连接同一个 HTTPS 云服务，已提交库存保存在 Neon PostgreSQL。电脑关机不影响云端库存操作。此仓库只包含程序、安装包和测试，不包含用户库存、数据库密码或 APK 签名私钥。

## 免费部署

使用 Render Free Web Service + Neon Free PostgreSQL。无需购买域名、持久磁盘或 Render 付费数据库。免费额度以平台当前页面为准；不要选择 Starter、付费磁盘或自动升级。

1. 在 Neon 免费项目点击 **Connect**，选择 PostgreSQL 连接串并复制。连接串以 `postgresql://` 开头，带有账号和密码。将它直接粘贴到下一步 Render 的环境变量，不要放到聊天、GitHub 文件、APK 或网页代码。
2. 在 Render 选择 **New → Web Service**，连接 GitHub 仓库 `Wayne1114884232/yuanjiancang`。若看不到仓库，在 GitHub 授权中允许 Render 访问该私有仓库。
3. 分支选择 `main`，Runtime/Language 选择 **Docker**，Instance Type 选择 **Free**。名称可填写 `yuanjiancang`；区域尽量与 Neon 项目接近。不添加持久磁盘。
4. 添加环境变量：

   | Key | Value |
   | --- | --- |
   | `DATABASE_URL` | Neon Connect 中完整的 PostgreSQL 连接串 |
   | `TRUST_PROXY` | `true` |
   | `NODE_ENV` | `production` |
   | `PORT` | `10000` |

5. 确认显示免费后，点击 **Deploy Web Service**。也可通过 **New → Blueprint** 读取 `render.yaml`，该文件已指定 `plan: free`，并要求在平台安全填写 `DATABASE_URL`。
6. 等待状态为 **Live**，打开 Render 给出的 `https://…onrender.com` 地址。`/api/health` 应显示 `version: 2.1.0-beta.2`、`mode: cloud-multiplayer`、`storage: postgresql`。

服务启动前必须连接数据库；未设置 `DATABASE_URL` 会拒绝启动，绝不会退回临时磁盘保存库存。数据库表在第一次成功启动时自动创建。

## 手机安装

打开部署地址下的 `/downloads/component-hub.apk` 下载并安装。APP 第一次启动时填写上面的 HTTPS 云端地址。无需设置电脑 IP，也无需 Tailscale。安装包使用多人版的独立应用身份，可更新多人测试 APP，不覆盖原单人 v1.7。

## 使用与旧库存迁移

每人注册自己的账号，系统会建立空的“我的库存”。可以创建“学校实验室”“项目组”等库，生成邀请码供朋友加入。账号权限决定能做什么，手机和电脑使用相同的权限。

在旧单人版或本地多人版下载所需库的 JSON 备份，然后在云端用你自己的账号进入空库，选择 **设置与备份 → 迁移旧库存到空库**。上传后会预览元件、库位、流水和总数量；人工确认才提交。其他人的个人库不会自动获得这份数据。源文件与原电脑库存保留，但两套服务此后独立记账。

账号登录、四角色、成员地点/项目范围、BOM入库自动建档、BOM出库、全选出库、审计、审批、设备撤销、备份恢复、预留负责人、采购负责人、邀请/加入申请、只读分享和跨库调拨继承多人版。开启敏感审批的库暂不支持跨库调拨。

## 同步与免费方案边界

- 成功写入先在 PostgreSQL 事务中提交，然后才向手机或电脑返回成功。数据库故障会报错，不会先写本机再延迟上传。
- 不同服务实例同时操作时也由数据库串行核对版本。BOM和跨库调拨整笔提交，失败整笔回滚。同请求重试不重复扣账。
- 前台在线设备通常在数秒内收到更新；实际延迟受网络与免费服务唤醒影响。应用切到后台会关闭实时连接以减少无用连接，回到前台重新同步。
- 免费服务闲置后可能休眠，首次请求可能较慢。短暂502或服务启动页面不会清除手机离线快照。免费额度耗尽或平台暂停时，实时联机不可用；不会自动购买付费资源。
- 断网时显示已确认快照和最近同步时间，可以记录草稿；恢复网络后核对再提交。账户被撤销后，在线会话失效，但已离线保存的数据无法远程擦除。
- Render临时文件系统只处理照片等临时文件。库存、账号与库级备份均在 Neon；服务重启或重新部署不会依赖本地库存文件。
- 照片识别在Linux云服务内使用Tesseract中英文识别，并删除临时照片。构建包含所需组件，实际识别质量须使用真实标签验收。报价目前为管理员手动刷新立创阶梯价；厂商消息、系统推送和定时价格检查尚未配置。
- 库存按库保存为 JSONB，账号与协作信息另外保存。适合少量成员试用；目前写入仍共用事务锁，未宣称可承载大规模并发。

## 备份

在“设置与备份”下载每个库的完整 JSON，另存到电脑或其他独立存储。库内备份只用于恢复当前库，不能替代独立备份。完整账号、成员及多个库的服务备份使用 PostgreSQL 标准备份工具，连接信息只通过环境变量提供。当前未开通收费备份服务，也未宣称 Neon 免费方案含永久备份保证。

## 本地开发与验证

Node.js 22.13或更新版本。`npm ci` 安装依赖，`npm test` 启动仅监听127.0.0.1的临时 PostgreSQL 数据库并运行域模型和云端检查。测试数据位于忽略提交的 `test-results/`。本地开发需要设置测试数据库连接串和 `ALLOW_LOCAL_DATABASE=true`；公网数据库始终校验证书。

当前已通过47项自动检查，包括真实PostgreSQL读写、两连接并发、注入写入失败后的回滚、跨库原子性、HTTP实时事件、BOM/全选最终扣账、旧库迁移、注册登录重启持久化和退出持久化。另通过页面事件模拟，核对账号/库缓存隔离及免费服务启动失败时的快照保留。原本地多人版的53项检查仍独立保留。

本地检查不代表 Neon 线上部署、Linux容器构建、浏览器视觉或Android真机验收已完成；需要在Render首次部署后查看构建结果并用电脑/手机各登录一个账号完成联机验收。
