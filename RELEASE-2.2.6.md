# 元件仓 2.2.6

针对 APP 内更新后安卓安装器提示“安装文件错误 / 安装包可能已损坏”，改进安装器兼容性；是否解决用户手机上的问题仍需实机确认。

- 更新文件改用应用内部专用目录保存，下载完成后再做 SHA-256、包名、版本号和签名校验。
- 改进系统安装器读取更新文件的授权和文件描述，避免部分手机无法读取 APP 内更新文件。
- 更新弹窗增加“浏览器下载”备用入口；如果手机系统仍阻止 APP 内安装，可以直接用手机浏览器下载后安装。
- 下载地址带版本校验参数，服务端对 APK 返回明确大小、附件名称和禁止缓存，避免读到旧文件或不完整缓存。

本版版本号 2.2.6，内部版本号 18，继续使用原签名，可覆盖 2.2.5。自动测试和 APK 签名验证完成；尚未在用户的具体手机型号上完成实机安装验收。
